import type { CaseLog } from './types';

// This file is now deprecated and will be removed in a future step.
// All data is now being fetched from Firestore.
export const mockCaseLogs: CaseLog[] = [];
